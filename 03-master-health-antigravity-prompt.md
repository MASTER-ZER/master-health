# برومت AntiGravity — Master Health (مقسم على مراحل)

## طريقة الاستخدام
انسخ "برومت البداية" أول مرة بس. بعد كل مرحلة يخلص منها AntiGravity، ارجعله بـ "أمر الاستمرار" (في آخر الملف) بدون ما تعيد شرح المشروع من الأول — هو المفروض يكون حافظ السياق. لو حسيت إنه نسي حاجة، ارجع وذكّره بجزء من ملف الفكرة (01-master-health-brief.md).

---

## برومت البداية (يُرسل مرة واحدة فقط)

```
أنت هتبني معايا مشروع اسمه "Master Health" — موقع لعيادة طبيب واحد فيه نظام حجز مواعيد بسيط.

السياق الكامل للمشروع (احفظه بالكامل طول الجلسة، هنرجعله كتير):

[هنا الصق محتوى ملف 01-master-health-brief.md كامل]

التصميم جاهز (تم عمله على Google Stitch) وهرفقهولك كصور/كود منفصل.

قواعد أمان مهمة جدًا قبل ما نبدأ:
- Supabase بيدي نوعين من المفاتيح: anon/public key و service_role secret key.
- الـ anon key بس هو اللي يُستخدم في كود الـ frontend (الكود اللي بيتنشر على Vercel ويشوفه أي حد).
- الـ service_role secret key ممنوع يظهر في أي كود frontend أو يترفع على GitHub أبدًا — لو احتجناه لأي عملية إدارية، هيكون في environment variable على السيرفر فقط (مش في الكود اللي بيوصل للمتصفح).
- لو مش متأكد نستخدم أنهي مفتاح فين، اسألني قبل ما تكمل.

هنقسم البناء على 5 مراحل، كل مرحلة نسبة من المشروع. مش عايزك تحاول تبني كل حاجة مرة واحدة — ركز بس في المرحلة الحالية وابذل أقصى مجهود فيها، واحفظ في دماغك إن فيه مراحل جاية بعدها.

---

**المرحلة 1 (0% → 20%): الإعداد الأساسي والهيكل**

المطلوب في المرحلة دي بس:
1. إعداد المشروع (Next.js أو أي framework مناسب حسب الـ setup الحالي بتاعي)
2. ربط Supabase (باستخدام الـ URL والـ anon key اللي هديهولك)
3. إنشاء جدول `bookings` في قاعدة البيانات بالأعمدة: id, patient_name, phone, preferred_date, preferred_time, note, status (default: pending), created_at
4. إنشاء جدول `admin_users` بسيط لحساب دخول واحد (أو استخدام Supabase Auth لو أسهل)
5. هيكل الصفحات الأساسي (routing) لكل الصفحات المطلوبة، حتى لو فاضية دلوقتي: الرئيسية، عن العيادة، احجز موعد، تواصل، تسجيل دخول، لوحة التحكم

لما تخلص المرحلة دي، وضحلي: إيه اللي اتعمل بالظبط، وإيه اللي لسه ناقص، وانتظر مني كلمة "كمل" قبل ما تروح للمرحلة اللي بعدها.
```

---

## أوامر الاستمرار (تُرسل بعد كل مرحلة)

**بعد المرحلة 1 (لبدء 20%→40%):**
```
تمام، كمل. المرحلة دي (20% → 40%): بناء الصفحات الأمامية (Home, About, Contact) بالتصميم الكامل حسب ملف الهوية البصرية اللي معاك (الألوان: أزرق #2B6CB0، أخضر #38A169، خلفية #F7FAFC). لسه ما تلمسش فورم الحجز ولا لوحة التحكم. ركز بس في الصفحات دي الثلاثة وخليها متجاوبة (responsive) وبـ RTL صح.
```

**بعد المرحلة 2 (لبدء 40%→60%):**
```
كمل. المرحلة دي (40% → 60%): بناء صفحة "احجز موعد" بالكامل — الفورم، الـ validation (رقم هاتف صحيح، تاريخ مش في الماضي)، وربطها فعليًا بجدول bookings في Supabase عشان الحجز يتخزن حقيقي. اعرض رسالة تأكيد واضحة بعد الحجز الناجح.
```

**بعد المرحلة 3 (لبدء 60%→80%):**
```
كمل. المرحلة دي (60% → 80%): بناء نظام تسجيل الدخول للأدمن (حساب واحد بس) ولوحة التحكم اللي بتعرض كل الحجوزات من جدول bookings في جدول واضح، مع إمكانية تغيير حالة الحجز (pending/confirmed/cancelled). لازم لوحة التحكم دي تكون محمية ومش متاحة لغير المسجل دخول.
```

**بعد المرحلة 4 (لبدء 80%→100%):**
```
كمل. المرحلة الأخيرة (80% → 100%): مراجعة شاملة — تأكد إن كل الصفحات متجاوبة على الموبايل، إن كل النصوص عربي RTL صح، إصلاح أي باجات ظاهرة، والتأكد إن مفيش أي secret key ظاهر في أي كود frontend. بعد كده جهزلي خطوات الرفع على GitHub والنشر على Vercel خطوة بخطوة.
```

---

**ملاحظة لإياد:** لو AntiGravity في أي مرحلة قال "خلصت" بس لما تجرب تلاقي حاجة ناقصة أو باظت، ارجعله بوصف دقيق للمشكلة بدل ما تقوله "مش شغال" بس — كل ما الوصف يكون محدد، كل ما الحل يكون أسرع وأدق.
